# Managed by Ansible role monitoring_server — generated from
# group_vars (grafana_admin_user / grafana_admin_password, which should be
# stored via ansible-vault, not plaintext, in a real deployment).
GRAFANA_ADMIN_USER={{ grafana_admin_user }}
GRAFANA_ADMIN_PASSWORD={{ grafana_admin_password }}
