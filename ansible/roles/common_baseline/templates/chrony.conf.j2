# Managed by Ansible role common_baseline — do not edit by hand
{% for server in baseline_ntp_servers %}
server {{ server }} iburst
{% endfor %}

driftfile /var/lib/chrony/drift
makestep 1.0 3
rtcsync
logdir /var/log/chrony
